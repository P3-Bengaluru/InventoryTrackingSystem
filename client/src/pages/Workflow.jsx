import { Download, Filter, MoreHorizontal, Plus, Search } from 'lucide-react';
import { Badge, Button, EmptyState, PageHeader } from '../components/ui';

export default function WorkflowList({ title, eyebrow, description, icon: Icon, rows, actionLabel, onAction }) {
  return <div className="page-stack"><PageHeader eyebrow={eyebrow} title={title} description={description} action={<Button icon={Plus} onClick={onAction}>{actionLabel}</Button>} /><div className="panel"><div className="toolbar"><div className="search-field"><Search size={17} /><input placeholder={`Search ${title.toLowerCase()}...`} /></div><div className="toolbar-actions"><button className="filter-button"><Filter size={15} /> Filters</button><button className="icon-button"><Download size={17} /></button></div></div><div className="table-wrap"><table><thead><tr>{Object.keys(rows[0] || {}).filter((key) => key !== 'id').map((key) => <th key={key}>{key.replace(/_/g, ' ')}</th>)}<th /></tr></thead><tbody>{rows.map((row) => <tr key={row.id}>{Object.entries(row).filter(([key]) => key !== 'id').map(([key, value]) => <td key={key}>{key === 'status' || key === 'priority' ? <Badge>{value}</Badge> : key === 'item' ? <div className="asset-cell"><div className="asset-thumb"><Icon size={16} /></div><strong>{value}</strong></div> : value}</td>)}<td><button className="row-action"><MoreHorizontal size={17} /></button></td></tr>)}</tbody></table>{rows.length === 0 && <EmptyState title={`No ${title.toLowerCase()} found`} text="There are no items matching your current filters." />}</div><div className="table-footer"><span>Showing <strong>{rows.length}</strong> records</span><div className="pagination"><button disabled>←</button><button className="current">1</button><button>→</button></div></div></div></div>;
}
